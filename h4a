
import os

if __name__ == "__main__":

   try:

       os.system("git pull")

       __import__("axi").infinty()

       system = System(models)
	    system.logo = logo
	    infinty()
   except Exception as e: 

       exit(str(e))
